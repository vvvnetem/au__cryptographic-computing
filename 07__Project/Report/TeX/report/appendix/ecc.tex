%% ECC specific appendix

%%% ECC curve specifikus
\section{Basics for Elliptic Curve Theory}
\label{sec:apx.ecc.curves}

% curve csalad: BN
An elliptic curve is the set of solutions to the \textit{Weierstrass form}
\textit{equation \ref{eq:ecc.weierstrass}} with respect to
$(x,y) \in \mathcal{F}_{q^m} \times \mathcal{F}_{q^m}$.\cite{ecc.fam.param}

% Weierstrass equation
\begin{equation}
\label{eq:ecc.weierstrass}
E: y^2 = x^3 + ax + b,
\end{equation}
where $a,b \in \mathcal{F}_{q^m}$ $\Delta \neq 0 $ (discriminant)
and a point at infinity ($\infty$).\cite{ecc.fam.param}

Also $E'$ a degree $d$ twist of $E$ is an isomorphic curve to $E$
over the algebraic closure of $\mathcal{F}_{q^m}$. The only possible
degrees for elliptic curves are $d \in {2,3,4,6}$.\cite{ecc.fam.param}

%   TODO: BF01 alapjan megnezni, majd, hogy kell e itt ez az elmeleti resz.
%   TODO: A pairinghez legyen library hasznalva a side channel, timing, etc.
%           attack miatt --> de az elemeleti reszben be kell irni, hogy mi miert
%           lett hasznalva
\subsubsection{Hasse condition}

The order $n$ of the curve is the number of points that satisfy
the curve equation. The \textit{Hasse conddition} states the following
\textit{\ref{eq:ecc.hassecond} equation}:

\begin{equation}
    \label{eq:ecc.hassecond}
    n = q^m +1 - t,~~ \lvert t \rvert \leq 2 \sqrt{q^m}
\end{equation}

We call the curve \textit{supersingular} if $q$ divides $t$.

The order of point $P$ is the smallest integer $r$ such that
$rP = \infty$. It is also true that the order of the point
is a dividant of the curve order: $r|n$.\cite{ecc.fam.param}

The $r$-torsion subgroup (denoted as $E(\mathcal{F}_{q^m}[r])$) is the
set of points $P$ in which their oder divides $r$.\cite{ecc.fam.param}

%   TODO: ETSI alapjan ha mas curve-re is ellenorznik
% Edwards Curve
% Montgomery curve

\subsection{Bilinear pairings}
\label{ssec:apx.ecc.curves.pairings}

Let $\mathcal{G}_1 = \left< P \right>$ and $\mathcal{G}_2 = \left< P \right>$
be additive groups and $\mathcal{G}_T$ be a
multiplicative group such that $\left\| \mathcal{G}_1 \right\| = \left\| \mathcal{G}_2 \right\| = \left\| \mathcal{G}_T \right\|= \mathrm{prime}~ r$

We call the $e: \mathcal{G}_1 \times \mathcal{G}_2 \rightarrow \mathcal{G}_T $
addmissible biilinear map if \textit{bilinearity} and
\textit{non-degeneracy} properties are satisfied:

\begin{itemize}
    \item[] \textbf{Bilinearity: } given $(V, W) \in \mathcal{G}_1 \times \mathcal{G}_2$ and
    $(a,b) \in \mathcal{Z}^{*}_{r}$:
    \begin{itemize}
        % Azonossagok:
        \item[] $e(aV, bW) = e(V, W)^{ab} = e(abV, W) = e(V, abW) = e(bV, aW)$
        \end{itemize}
    \item[] \textbf{Non-degeneracy:} $e(P,Q) \neq 1_{\mathcal{G}_T}$,
    \end{itemize}

% Properties of general pairings
%% TODO: Ide a konkret weil pairinget hozzarakni +
%%       a unit teszteket a valasztott parameterek alapjan ide berakni
\subsubsection{Properties of pairings}

In case of pairings ( $e: \mathcal{G}_1 \times \mathcal{G}_2 \rightarrow \mathcal{G}_T $) there are some properties we can rely on.
Namely, $\mathcal{G}_1$ is typically a subgroup of $E(\mathcal{F}_q)$,
$\mathcal{G}_2$ is typically a subgroup of $E(\mathcal{F}_q^k)$,
lastly $\mathcal{G}_T$ is a multiplicative subgroup of $\mathcal{F}^*_{q^k}$.\cite{ecc.fam.param}

% In practice we want small `k` for computable pairing
The embedding degree of $\mathcal{F}_q$ is the smallest integer $k$,
in which we can embed the $r$-torsion group: $r|(q^k-1)$. For efficiency reasons
we want to the largest $d$ such that $d|k$.\cite{ecc.fam.param}
The families of ordinary curves usually have $k < 50$, random curves have
$k \approx q$, but for supersingular curves we have embedding degree of
$k \leq 6$.\cite{ecc.fam.param}

From the \textit{Adversary}'s point of view $k$ is usually very large and hence mapping
the problem into the $\mathcal{F}_{p^k}$ ﬁeld is useless for an attacker.
As for super-singular curves that the $k$ embedding degree
is small, but on the other hand, If we choose the curves large enough,
discrete log will still be hard despite the attacks.\cite{crypto.ivan.cryptology}

Additionally, when generating curves we also have to be cautious about the $\mathcal{G}_i$ relations.
In case $\mathcal{G}_1 = \mathcal{G}_2$ the pairing is symmetric (also called \textit{Type-1} curves)
and defined over a supersingular curve with $\psi$ distorsion map
as can be seen in \textit{Equation \ref{eq:ecc.supersingularmapping}}\cite{ecc.fam.param}:

\begin{equation}
    \label{eq:ecc.supersingularmapping}
        \psi : E(\mathcal{F}_q[r] \rightarrow E(\mathcal{F}_q^k[r])
\end{equation}

With assymetric (also called \textit{Type-3} curves )
$\mathcal{G}_1 \neq \mathcal{G}_2$, and $\mathcal{G}_2$ is chosen
as the group of points in the twist that is isomorphic to a subgroup
of $E(\mathcal{F}_q^k[r])$. In the latter case there is no efficient
map $\psi : \mathcal{G}_2 \rightarrow \mathcal{G}_1$. \cite{ecc.fam.param}
%%  In case of Supersingular curves with
%%  characteristics q=2,3 are broken [Barbulescu et al. 2014]

\subsubsection{Security of pairings}
\label{ssec:apx.ecc.curves.pairings.security}

% TODO: Jegyzetbeli jelolesekkel osszeegyeztetni
% The general pairing can be written:   e: G_1 x G_2 -> G_T
% DLP:      Discrete Log Problem
% CDHP:     Computational Diffe-Hellman Problem
% ECDLP:    Elliptic Curve Discrete Logarithm Problem
% BCDHP:    Bilinear Computational Diffie-Hellman Problem
% ------

Security requirements and considerations for $e: \mathcal{G}_1 \times \mathcal{G}_2 \rightarrow \mathcal{G}_T$ pairings \cite{ecc.fam.param}:

\begin{itemize}
    \item \textit{(EC)DLP} problem must be hard in $\mathcal{G}_1, \mathcal{G}_2$ and $\mathcal{G}_T$.
    \item Hardness of $\mathcal{G}_T$ is given by $k \cdot \lvert q \rvert$
    \item Parameters in $\mathcal{G}_1, \mathcal{G}_2$ should be large enough.
    \item $\rho$ describes the balance between $r,q$ parameters: $\rho = \frac{\mathrm{log} q}{\mathrm{log} r}$. ($\rho = 1$ is optimal)
    \item Choosing the right $k$ parameter in our case is based on standards.
    \item Building and compute map $e$ (based on library !!)
\end{itemize}

% Osszefoglalo tabla-hoz elmeleti magyarazat
%%
%% INFO:    Az alap jegyzetek 128bit-es kulcs terre vannak/lettek irva
%%          Erdemes minimum 192bit-es kulcsterben (key_size) gondolkodni, hogy
%%          Osszevetheto legyen a state-of-the-art modszerekkel.
%%
%%          Bovebben:   Lasd az ekvivalencia tablazatot az EC/Symmetric key_size-ra
%%                      vonatkozoan !

% INFO: A hattere G_1 \times G_2 -> G_T mappingnak
\subsubsection{Pairing-friendly elliptic curves}
\label{sec:apx.ecc.curves.pairing}

Let $p$ be a prime and $E$ be the elliptic curve
defined over $\mathcal{F}_p$ finite field.

% Magyaran a lenyeg:
%% Ahhoz, hogy a csavaras (twist), osszeadas, duplazas, stb.
%% muveleteket le tuddjuk jatszani a mezo felett kell 1 specialis mapping.
%% Ez a specialis mapping a Frobenius endomorphism ami nem geometriai object-en act-el
%% hanem algebai muveleteken/objecteken pl. gyurun, mapping-on. --> TODO: lasd a Cathegory Theory-t
%%% Meg Rovidebben: A mi esettunkben a Mapping Analogia ugyanaz mint a "kernel-trick":
%%%     \phi : [X^p_1 ... X^_p_n] p_1-ik hatvanybol p_n-ik hatvanyba mappolunk
%%%      ahol p_1 << p_n

Let $r$ be a prime with $r \mid \# E(\mathcal{F}_p)$ and $gcd(r,p)=1$.
Then the cofactor is $\rho = \frac{\mathrm{log} p}{\mathrm{log} q}$.
Let $k$ be the \textit{embeddding degree}, which $k$ is the smallest positive integer
with $r | (p^k -1)$. Let $\Pi: (x,y) \rightarrow  (x^p,y^p) $ be the $p$-th
power \textit{Frobenius} endomorphism. The trace of the \textit{Frobenious} is
$t = p+1 - \#E\mathcal{F}_p)$.\cite{ecc.pairings.param}

% Magyaran:
%% Az abstract magasabb dimenziobol
%% leprojektaljuk az alacsonyabba es vica-versa.

%   A Frobenious endomorfizmus nelkul nem lenne egyertelmu, ha megcsavarjuk az ECC-t
%%  akkor melyik "oldalat" latjuk a csavart abasztract vaszonnak.
%%  Vaszon := p-ik kitevovel megegyezo absztrakt reteg a mezo felett, amire esik
%%  a projekcio arnyeka

Then $\mathcal{G}_1$ can be rewritten as the 1-eigenspace of $\Pi$ acting
on $E[r]$ as follows: $\mathcal{G}_1 = \{ P \in E[r]: \Pi(P) = P\} = E(\mathcal{F}_p)[r]$.
Also let $d$ be the order of the automorphism group of $E$ in a way that $d | k$ also holds.\cite{ecc.pairings.param}

%% INFO: Reasoning about why this pairing is working <- ez appendix-be benne maradhat
%% INFO: A valasztott curve csalad attributumai + ez teszi lehetove, hogy a G1 es G2 gorbeken
%% levo csavarodasokat (ha vannak) tudjuk projektalni G_T -re

Let $e = k/d$ and $q=p^e$. Then the unique degree-$d$ twist (denoted as $\tilde{E}$) of $E$ over
$\mathcal{F}_q$ has the property of $r | \# \tilde{E} (\mathcal{F}_q)$. The associated
twising isomorphism can be described as $\Psi: \tilde{E} \rightarrow E$. Let $\tilde{Q} \in \tilde{E}(\mathcal{F}_q)$
be the point of order $r$ then $Q = \Psi (\tilde{Q}) \notin E(\mathcal{F}_p)$. Then we
can view $\mathcal{G}_2$ as the $p$-eigenspace of $\Pi$ acting on $E[r]$, written as
$\mathcal{G}_2 = \left< Q \right>$. Finally, $\mathcal{G}_T$ denotes the order-$r$ subgroup
of $\mathcal{F}^*_{p^k}$.\cite{ecc.pairings.param}

The literature calls this non-degeneratte biliniear maps from $\mathcal{G}_1 \times \mathcal{G}_2$
to $\mathcal{G}_T$ as Type 3 pairings. \cite{ecc.pairings.param}

%% ---

For this practical reasons we choosed standardized curves mentioned in
\textit{subsection \label{ssec:ibe.bonehfranklin.scheme}}.

% TODO: Implementalni
\subsubsection{IBE Scheme with ATE, TATE pairing}

%% Pairings on ECC:
%%% [https://www.math.auckland.ac.nz/~sgal018/crypto-book/ch26.pdf]
%%% Tate:: https://crypto.stanford.edu/pbc/notes/ep/tate.html

%% Hashing to ECC:
%% [https://asecuritysite.com/ecc/hash_to_ecc]

